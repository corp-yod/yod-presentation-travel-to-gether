name: yod_presentation_travel_to_gether
description: "A new Flutter package project."

publish_to: "none"

version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  yod_navigator:
    path: ../yod-navigator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
