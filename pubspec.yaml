name: yod_presentation_travel_to_gether
description: "A new Flutter package project."

publish_to: "none"

version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  yod:
    git:
      url: https://github.com/corp-yod/yod.git
      ref: develop

  yod_navigator:
    git:
      url: https://github.com/corp-yod/yod-navigator.git
      ref: main

  yod_nak_ram_ui_kit:
    git:
      url: https://github.com/corp-yod/yod-nak-ram-ui-kit.git
      ref: develop

dependency_overrides:
  yod:
    path: ../yod
#   yod_nak_ram_ui_kit:
#     path: ../yod-nak-ram-ui-kit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  coverage: ^1.15.0

flutter:
